const db = require("../../config/db.config");

module.exports = class Admin{
    constructor(
        admin_id,
        admin_name,
        username,
        admin_password,
        mobile_number,
        admin_email,
        user_type,
        status,
        subroles,
        admin_address,
        address_let,
        address_long,
        dateadded,
        dateupdated,

        )
}
